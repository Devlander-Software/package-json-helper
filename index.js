const {updatePackageJsonType}  =require("./update-package-json-type")
// TO DO:
// check if it is being ran from the root of the project and the command line
// and if it is, check the process.arg commands and run the function with the correct arguments

module.exports = {
    updatePackageJsonType
}